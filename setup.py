from setuptools import setup, find_packages

setup(
    name="chemistry-recognizer",
    version="1.0.0",
    packages=find_packages(),
    description="Chemical compound structure recognition for bachillerato students",
    python_requires=">=3.10",
)
