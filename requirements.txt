# Dependencias minimas para desplegar la demo (streamlit_app.py) en
# Streamlit Cloud o Hugging Face Spaces. Sin Jupyter, sin DECIMER (que arrastra
# TensorFlow), sin papermill ni umap. Para desarrollo local con todos los
# notebooks usa `requirements-dev.txt` o el entorno conda `environment.yml`.

streamlit>=1.34
streamlit-drawable-canvas>=0.9.3

# Modelo
torch>=2.1,<3.0
torchvision>=0.16,<1.0
numpy>=1.26,<2.3
pandas>=2.1
Pillow>=10.1

# Augmentation pipeline (usado en VAL_TRANSFORM tambien)
albumentations>=1.3

# Render canonico de moleculas
rdkit>=2023.9
