# Dependencias minimas para desplegar la demo (streamlit_app.py) en
# Streamlit Cloud. Sin Jupyter, sin DECIMER (arrastra TensorFlow), sin
# papermill ni umap. Para desarrollo local con todos los notebooks usa
# requirements-dev.txt o el entorno conda environment.yml.

# torch CPU-only (en Streamlit Cloud no hay GPU). Reduce el wheel de
# torch de ~800 MB a ~150 MB y permite encajar en el free tier de 1 GB.
--extra-index-url https://download.pytorch.org/whl/cpu

streamlit>=1.34
streamlit-drawable-canvas>=0.9.3

# Modelo
torch==2.3.1+cpu
torchvision==0.18.1+cpu
numpy>=1.26,<2.3
pandas>=2.1
Pillow>=10.1

# Augmentation pipeline (VAL_TRANSFORM usado en inferencia)
albumentations>=1.3

# Render canonico de moleculas
rdkit>=2023.9
